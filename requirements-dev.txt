# Development and test dependencies (in addition to requirements.txt).
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<10.0
# Required by fastapi.testclient (starlette's TestClient is backed by httpx).
httpx>=0.27,<1.0
